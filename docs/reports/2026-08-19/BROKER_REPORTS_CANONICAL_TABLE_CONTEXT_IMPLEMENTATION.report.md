# Внедрение табличного контекста в Canonical

Дата: 2026-08-19

Статус: механизм и дополнительный fail-closed guard внедрены, проверены на
сервере и сведены в `main`

## Боль

PDF-парсер хорошо сохранял символы, но часто терял структуру таблиц. VLM видела
таблицы, но не должна была переписывать значения. Старый текстовый путь сохранял
слова, но не являлся рабочим fallback для таблицы.

## К чему пришли

Оставлен один маршрут:

1. VLM указывает только границу каждой визуальной таблицы.
2. Координаты переводятся в систему PDF.
3. Pdfplumber читает строки, столбцы, ячейки и значения из исходного PDF.
4. Существующий владелец табличной проекции проверяет результат.
5. Canonical публикует проверенные физические таблицы и их исходные ссылки.

Модель не возвращает текст, не выбирает настройки парсера и не получает права
исправлять таблицу по смыслу.

## Где была ошибка

После правильной навигации оставались три механические проблемы:

- координата модели иногда обрезала внешнюю линию таблицы на долю PDF-пункта;
- код превращал список ячеек pdfplumber в плоский список и терял пустые слоты,
  объединённые заголовки и диапазоны;
- продолжения одной таблицы на соседних страницах не имели явной связи.

Дополнительные отчёты показали четвёртую проблему: pdfplumber иногда создавал
полностью пустые промежуточные строки или столбцы из особенностей оформления.

## Что изменено

- Парсеру дан фиксированный запас в 1 PDF-пункт только для чтения внешней
  границы. Источником слов остаётся исходная область VLM.
- Сохраняются нативные слоты, `row_span`, `column_span` и объединённые ячейки.
- Удаляются только те оси сетки, в которых нет ни одного исходного слова.
- Сегменты на соседних страницах связываются только по строгим признакам:
  край страницы, одинаковая сетка столбцов, заголовок у начала и отсутствие
  заголовка у продолжения.
- Связь имеет отдельный проверяемый контракт. Строки и значения физически не
  склеиваются, смысл таблицы не угадывается.
- Canonical переносит объединённые диапазоны и связь продолжения в метаданные
  `TABLE`.

## Проверка на исходном реальном отчёте

Заморожены те же 14 координат VLM; повторного вызова модели и подгонки prompt не
было.

- 14 из 14 областей стали готовыми табличными проекциями;
- 14 физических `TABLE` опубликованы в Canonical;
- они представляют 12 логических таблиц: две пары сегментов строго связаны
  через границу страницы;
- сохранено 2 032 ячейки, включая 32 объединённые;
- у каждой ячейки сохранена исходная привязка;
- табличного блокера нет.

Серверный прогон подтвердил тот же результат уже в полном рабочем пути:

- 14 физических и 12 логических таблиц;
- 2 032 ячейки, включая 32 объединённые;
- 2 032 из 2 032 ячеек имеют разрешённую исходную ссылку;
- 5 307 из 5 307 проверенных слов и чисел найдены на привязанной странице PDF;
- визуальная сверка всех шести страниц совпала с формой Canonical;
- Canonical прошёл штатный валидатор без ошибок.

Сам Gate 2 завершился правильно. Следующий налоговый этап остановился из-за
неполной роли документа. Это отдельная downstream-проблема, не ошибка таблиц.
Первый вариант стенда смешивал эти два результата и поэтому ошибочно помечал
весь прогон как неуспешный. Стенд разделён по контрактной границе.

Отличие от исходного аудита: раньше готовыми были 5 из 14 областей, остальные
теряли табличную форму или блокировались.

## Проверка на двух других отчётах

Использованы пять заранее замороженных страниц двух публичных брокерских
отчётов: три положительных и две отрицательных. Новых вызовов модели не было.

- на положительных страницах получено ровно 3 из 3 таблиц;
- на отрицательных страницах ложных таблиц нет;
- таблица с визуальными разделителями восстановлена как 15×3, без 12 ложных
  пустых строк;
- небольшая таблица восстановлена как 4×2, без 4 ложных пустых столбцов;
- длинная текстовая таблица сохранила три настоящих столбца и весь порядок
  текста.

После серверного внедрения дополнительно выполнено по одному новому проходу на
двух целых публичных брокерских отчётах, без retry и best-of-N.

Первый отчёт прошёл:

- одна область оглавления и одна финансовая таблица остались отдельными;
- финансовая таблица сохранена как 21×3;
- всего в Canonical 72 ячейки, 72 исходные ссылки разрешены;
- 146 из 146 проверенных слов и чисел найдены на привязанных страницах;
- остальные страницы просмотрены, пропущенных таблиц нет;
- Canonical валиден и активирован.

Второй отчёт дал честный отрицательный результат:

- VLM указала три области;
- одна область была оглавлением, одна настоящая таблица оказалась изображением,
  ещё одна не имела достаточной явной сетки для текущего строгого сборщика;
- PDF-парсер не опубликовал ни одной табличной проекции;
- стенд правильно зафиксировал несовпадение `3 != 0`.

При этом обнаружена интеграционная ошибка: блокер
`pdf_table_normalization_incomplete` уже существовал, но кандидат Canonical без
таблиц всё равно мог быть активирован. Внедрён узкий guard в существующем
владельце публикации: такой документ больше не публикует кандидат Canonical,
downstream блокируется, легаси не включается. Отдельный парсер, схема или
fallback не добавлены.

Пост-релизная проверка подтвердила обе стороны контракта:

- поддержанный отчёт снова дал 2 из 2 проекций и активный валидный Canonical:
  две отдельные таблицы, 72 ячейки, 72 разрешённые исходные ссылки и 146 из 146
  слов/чисел на привязанных страницах;
- неподдержанный отчёт снова дал 3 области и 0 проекций, но теперь создал 0
  версий Canonical; записана явная причина
  `canonical_pdf_table_normalization_incomplete`, downstream заблокирован,
  `legacy_fallback_used=false`.

## Честное ограничение

Если внутри длинной ячейки нет явной границы строки, переносы текста могут
остаться отдельными физическими строками. Система сохраняет столбец, порядок,
исходные значения и низкую оценку качества, но не склеивает строки по смыслу.
Это сознательное fail-closed поведение.

Таблица, состоящая только из изображения, не поддерживается этим маршрутом:
pdfplumber не может отдать точные исходные символы. Система должна остановить
такой документ, а не создать Canonical без таблицы и не переписать числа через
VLM.

## Что не является решением

- повторная настройка prompt после просмотра результата;
- сетка поверх изображения;
- текст или числа, переписанные VLM;
- best-of-N, повторный запрос или ручная правка;
- старый плоский текстовый маршрут под названием fallback;
- отдельная схема или второй владелец Canonical.

## Локальная проверка

- профильные тесты PDF/проекции/Canonical: 61 passed;
- архитектурные проверки: 31 passed;
- bundle и PDF contract: 14 passed;
- официальный Context V2.1 CI-профиль: 339 passed, 3 skipped, затем 9 passed;
- основной Broker Reports CI-профиль: 706 passed;
- Canonical и Gate 3–5 guards: 288 passed;
- проверки управляемых assets, bundle parity и Ruff: passed.

После добавления запрета на публикацию неполного табличного Canonical:

- узкий тест нового fail-closed поведения: passed;
- связанные PDF, Canonical, bundle, архитектурные и NDFL-тесты: 175 passed;
- полный основной профиль: 707 passed;
- полный профиль Canonical и Gate 3–5: 288 passed;
- Ruff: passed.

Неограниченный запуск всех 3 787 собранных тестов не вернул ошибку, но был
остановлен общим 20-минутным лимитом. Он не засчитан как зелёный. Вместо него
полностью повторены три точных набора, которые запускает `broker-reports-ci`.

## Поставка

- PR `#287`, GitHub Broker Reports CI: passed;
- merge-ревизия: `91df75d2a4f143cce2ca05a18c73cde48c54fd29`;
- серверный release: `broker-reports-91df75d2a4f1`;
- release manifest SHA-256:
  `85b6811523aa43f352934028d6c97feee3cd54017e123cbcd5e945633407b198`;
- rollback identity SHA-256:
  `ae6bc4446e5a1e2e8b806d7569934ef933de959b9e05be33b03317748eaf08d5`;
- независимый серверный verifier: passed;
- все три Function bundle и runtime identity совпадают с репозиторием;
- release staging пуст, рабочие счётчики документов, файлов, Knowledge и
  векторного хранилища не изменились.

Локальный процесс поставки не успел получить итоговую квитанцию в двухминутный
лимит, пока сервер завершал перезапуск и rollback-проверку. Повторной поставки
не было: состояние сначала проверено только чтением, затем независимый verifier
подтвердил завершённый релиз и точную ревизию.

## Итог

Поддержанные таблицы входят в Canonical как отдельные структурные объекты с
исходными координатами и значениями. Если хотя бы одна найденная таблица не
может быть собрана из исходного PDF, Canonical для документа не публикуется.
Ни плоский текст, ни легаси не считаются fallback.
