# Broker Reports restricted-scope audit final

Date: 2026-07-19

Result: completed

## Plain answers

- **How many candidates are unique material tables?** `80` are genuine,
  non-overlapping likely-material table regions. Only `14` broker regions are
  unique to the candidate representation after considering paired XML;
  `66` withholding tables have neutral XML equivalents.
- **Duplicates or false positives?** Candidate duplicates `0`, overlap pairs
  `0`; false-positive table candidates `90`. Another `24` canonical-projection
  candidates are form blocks rather than physical tables.
- **Deterministic recovery?** `180` withholding PDF candidates can be bypassed
  through a deterministic typed adapter over `24` XML packages. `33` candidate
  projections already exist but are not current-scope eligible. `51` visual
  units have text/layout fallback. The `14` broker regions still need Gate 1
  topology work.
- **Visual model required?** Not proven for any scope. `6` current visual units
  (`5` unique) and `6` additional document-restricted pages require visual-capable
  recovery. OCR/geometry must be attempted before bounded VL.
- **Safe deferral?** All `180` paired withholding PDF candidates, `51` visual
  fallbacks for text-bounded scenarios, `10` non-material visuals and `9`
  non-material headings. The underlying XML semantic adapter is not deferable
  for a complete withholding workflow.
- **Documents inadequate today?** `5` current visual-source documents plus one
  document-level restricted broker source require visual recovery; one other
  broker PDF is materially incomplete because of `14` candidates. The `24`
  PDF/XML withholding pairs require a typed XML consumer for complete financial
  interpretation.
- **Main ownership gap?** Gate 1 representation for broker PDF/visual structure;
  Gate 2 consumption for FNS XML semantics.
- **Narrowest high-value goal?** A versioned deterministic FNS 2-NDFL Gate 2
  adapter for the `24` neutral packages, preceded or followed separately by the
  small ZIP lineage handoff correction.
- **Provider impact of that goal?** `0` calls and `$0` provider cost. A later VL
  ceiling for all `11` unique visual scopes is `11` single-model calls or `22`
  dual-model calls; historical estimates are `$0.07033` or `$0.42257`
  respectively, before human review.
- **Can useful Gate 2 work begin now?** Yes for complete declared CSV/HTML
  scopes and bounded text-local PDF scopes. No complete-corpus interpretation
  claim is justified.

## Delivery boundary

This audit added only analytical tooling, tests and safe reports. It did not
promote candidates, weaken validators, add providers, create a production visual
consumer, change financial fact contracts or alter Gate 3. Stage delivery was
not required because no runtime or bundle changed.

Private images, resolver identities and operator material remain ignored under
the ignored private evidence root. The tracked safe inventory has no
customer values, filenames, private paths, source coordinates or raw media.

## Recommended next goals

1. `Broker Reports FNS 2-NDFL Typed Gate 2 Adapter`.
2. `Broker Reports Archive Lineage Handoff Correction` as a separate small
   contract-hygiene slice.
3. `Broker Reports Gate 1 Broker PDF Neutral Table Reconstruction` for `14`
   bounded regions.
4. `Broker Reports Bounded Visual Table OCR/VL Benchmark And Review Contract`
   for `11` unique material scopes.

The first goal is ready without further research.
