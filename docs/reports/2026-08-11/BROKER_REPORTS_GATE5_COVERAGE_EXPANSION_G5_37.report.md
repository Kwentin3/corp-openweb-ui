# Broker Reports Gate 5 G5.37 — Coverage Expansion Loop

Date: `2026-08-11`

Status: `PROVEN`

Terminal:

```text
COVERAGE_EXPANSION_LOOP_PROVEN
```

Да. Одна и та же Gate 1→5→XML вертикаль приняла CSV, HTML и XLSX с одним
`SECURITY_DISPOSAL`, свела их к эквивалентной Gate 4 и declaration semantics,
а новый binary multi-sheet XLSX прошёл настоящий OpenWebUI upload/chat/provider
path до повторно скачанного official-XSD-valid XML. Downstream tax/declaration
owners не менялись.

Безопасный receipt:
[G5.37 receipt](./BROKER_REPORTS_GATE5_COVERAGE_EXPANSION_G5_37.receipt.safe.json).

Контракт:
[Coverage Expansion Loop v0](../../stage2/contracts/BROKER_REPORTS_GATE5_COVERAGE_EXPANSION_LOOP.v0.md).

## 1. First-wave corpus

Versioned corpus:
[`g537_coverage_corpus.v0.json`](../../../services/broker-reports-gate1-proof/tests/fixtures/g537_coverage_corpus.v0.json),
version `2026-08-11.0`.

| Sample | Origin | Format | Meaning | Privacy |
| --- | --- | --- | --- | --- |
| G5.36 baseline | synthetic, not broker-attributed | CSV | `SECURITY_DISPOSAL` | no customer data |
| Russian vocabulary pressure | synthetic, not broker-attributed | HTML, two tables | `SECURITY_DISPOSAL` | no customer data |
| Multi-sheet pressure | deterministic synthetic, not broker-attributed | XLSX, two sheets | `SECURITY_DISPOSAL` | no customer data |
| T-Bank official sample | official public broker document | text/table PDF | `SECURITY_PURCHASE`, charges | raw bytes private/local only |

The public source is the T-Bank document-help page and its linked official
[sample PDF](https://cdn.tbank.ru/static/documents/7b9ccdee-5a02-4ed6-9499-c76082cd8d30.pdf).
Its SHA-256 is
`25c3b0606ce86852f6ac8fdf6feccbefedb609bcffc5c1581dc95b9b81c5da67`;
the raw PDF was not added to Git. Synthetic fixtures are explicitly marked and
are not presented as real broker reports.

## 2. Coverage ledger

| Sample | Initial terminal/blocker | Root cause | Changed owner / minimal fix | Final terminal | Downstream changes |
| --- | --- | --- | --- | --- | --- |
| baseline CSV disposal | existing `REAL_PRODUCT_PATH_XML_VALID` | none | none | `REPORT_SUPPORTED + REAL_PRODUCT_PATH_XML_VALID` | `NONE` |
| HTML Russian disposal | Gate 1 `completed_with_blockers`: advisory `unknown_role` | no blocking gap on the actual persisted product route | none; retain deterministic Gate 3 checks | `REPORT_SUPPORTED + LOCAL_END_TO_END_FULL_TARGET_XML_VALID` | `NONE` |
| XLSX multi-sheet disposal | `gate5_product_synthetic_utf8_source_required` | `PRODUCT_INTEGRATION_GAP`: proof source envelope assumed UTF-8 | existing G5.35 envelope accepts exactly one strict hash-bound UTF-8 or canonical base64 representation; product adapter selects it; browser upload uses actual MIME | `REPORT_SUPPORTED + REAL_PRODUCT_PATH_XML_VALID` | `NONE` |
| official T-Bank PDF | Gate 1 structurally complete but advisory `unknown_role`; live path asks for structured facts | `FINANCIAL_SEMANTIC_ROLE_COMPLETENESS_GAP` | none; no forced disposal/tax mapping | `STRUCTURAL_EXTRACTION_SUPPORTED + STRATEGIC_STOP_BEFORE_GATE4` | `NONE` |

The PDF reached native upload, live provider and validated Gate 3 persistence.
The private audit was reduced to safe aggregates: three annotations — one
`SECURITY_PURCHASE` and two `TRANSACTION_CHARGE`. Required purchase roles
`quantity`, `amount` and `unit_price` were missing, so no Gate 4 fact, Tax
Model or XML was claimed. This is the expected fail-closed outcome for the
first real/public semantic-pressure sample.

## 3. Minimal implementation

The only runtime coverage correction is a representation envelope:

```text
UTF-8 source → content_utf8
binary source → strict canonical content_base64
both → exact declared source SHA-256
```

The existing product adapter remains the owner and still delegates to
`Gate5EndToEndFullTargetXmlRuntime.continue_from_validated_gate3`. The same
browser proof script now passes the source filename and real MIME type into
native `File`/`FormData`. No second endpoint, parser, provider client or XML
path was created.

The deterministic XLSX builder uses fixed ZIP timestamps and stored entries;
its output is hash-pinned and regenerated byte-for-byte in tests.

## 4. Canonical convergence

Local product-route replay began with actual CSV/HTML/XLSX bytes, persisted
Gate 1 results, resolved each canonical artifact through the existing Gate 2
reader, ran Gate 3 proposal/schema/literal/source/role validation, rebuilt Gate
4 and traversed all existing Gate 5 owners.

All three representations converged to:

```text
financial_type = SECURITY_DISPOSAL
status = role_complete
date = 2025-02-11
asset = ASSET-G536
quantity = 1
amount = 100.00
currency = RUB
unit_price = 100.00
```

Gate 4 business semantics, Declaration Semantic Input domains and XML were
equivalent. Local XML was byte-identical for all three samples:
`07d2a96d89776d71877bdd1f30ce142a4c6b6f905e09d3e8bcfe238195a8ef2a`.

The model boundary in this deterministic convergence test supplies proposals;
the SUT still executes the real factory route and validators. No Gate result,
Tax Model, package, semantic input or XML fixture is mocked or prebuilt.

## 5. Real OpenWebUI product proof

Controlled staging used the same stable Workspace Model and Pipe as G5.36.
Built-in Playwright MCP was unavailable, so the compatible installed local
Playwright `1.59.1` Chromium path was used for real browser login and rendered
download.

| Boundary | Baseline CSV | New XLSX |
| --- | --- | --- |
| Native authenticated upload | passed | passed |
| Live configured Gate 3 provider | passed | passed |
| Machine supplied-case blocker | passed | passed |
| Missing `declaration_date` blocker | passed | passed |
| Gate 4→5→PROJECT | passed | passed |
| Rendered native download | passed ×2 | passed ×2 |
| Download bytes | `1109` | `1109` |
| Download SHA-256 | `69a2185184ec9c076c8557defd746f04a664caa6fcbc457f494eae1b0b0b16f6` | same |
| Replay byte-identical | yes | yes |
| Packaged official XSD | valid | valid |

The CSV and XLSX downloaded XML are also byte-identical across formats. The
XSD SHA-256 is
`083128322833dbafb29be0bf919e33b7b362956244b422d34dd554c99a1e4484`.

User B remained unable to read User A's objects: source `404`, XML `404`, chat
`401`. Missing source values still produced no XML.

## 6. Coverage map

| Source / format | Disposal | Purchase | Charge | Status |
| --- | --- | --- | --- | --- |
| synthetic CSV | full live XML | — | — | `REPORT_SUPPORTED` |
| synthetic HTML | full local XML | — | — | `REPORT_SUPPORTED` |
| synthetic XLSX | full live XML | — | — | `REPORT_SUPPORTED` |
| official T-Bank PDF | — | Gate 3, roles incomplete | Gate 3 | `STRATEGIC_STOP_BEFORE_GATE4` |

No blank cell is an inferred claim.

Useful metrics:

```text
NEW SYNTHETIC PRESSURE REPORTS SUPPORTED WITHOUT DOWNSTREAM TAX CHANGES = 2
NEW REAL/PUBLIC REPORTS FULLY SUPPORTED = 0
NEW DOCUMENT CLASSES WITH REAL_PRODUCT_PATH_XML_VALID = XLSX
```

## 7. Regression, anti-drift and KISS

Broad local matrix from the service cwd:

```text
287 passed
0 failed
6 known deprecation warnings
```

The matrix covers all Gate 5 tests plus product Pipe, generated bundle,
ArtifactStore, architecture stabilization and canonical multi-format tests.
The three new coverage tests separately prove corpus hashes/build
determinism, canonical convergence and no source vocabulary below Gate 4.

```text
broker-specific adapter: NO
broker-specific tax branch: NO
new Tax Model: NO
Declaration Definition change: NO
Projection/XML mapping change: NO
validator weakening: NO
new DB/registry/framework: NO
second pipeline: NO
```

KISS answer: the change expands the set of bytes accepted by the existing
source envelope and exercises existing normalization/semantic owners. It does
not build a separate T-Bank, XLSX or Russian-vocabulary system.

## 8. Cleanup and limitations

After the bounded live window, a second read-only check confirmed:

```text
Function global state restored: yes
Workspace Model grants restored: yes
all valves restored: yes
proof/audit valves enabled: no
temporary users removed by cleanup: yes
```

The 11-file implementation manifest SHA-256 is
`7a4cb780ac88a467a79649e1eca9d944581f19a75114c702ce5e434a1fec01e6`;
the deployed generated bundle SHA-256 is
`ae742f7522bf6e44b34b8f3eae605f73704e1e2b8047b5181bc44a44ff03a88b`.

This proves the first coverage-loop mechanism, not all brokers, all PDFs, all
economic meanings or taxpayer tax completeness. The full-success new inputs
are synthetic pressure documents. The official public PDF proves realistic
structural and Gate 3 pressure only; its incomplete purchase semantics were
not repaired speculatively. There was no production-wide activation, FNS
submission, commit, push or PR.

## 9. Terminal verdict

```text
MORE HETEROGENEOUS INPUTS
→ SAME CORE PIPELINE
→ SAME NORMALIZED DISPOSAL
→ SAME TAX/DECLARATION RUNTIME
→ NEW XLSX REAL PRODUCT XML
→ OFFICIAL XSD

COVERAGE_EXPANSION_LOOP_PROVEN
```

G5.37 завершён. Автоматического перехода к следующему GOAL нет.
