# DOC29 Infrastructure Reuse and Durable Contour — Brief

`BLOCKED`: deployment and reusable storage pattern were found, and the full
16-document contour passed locally with application-consistent isolated
restore, research rollback and 6x3 Wave 2 shadows. The first bounded target
backfill returned no receipt and made application health plus SSH banners
unavailable.

Selected authority: existing Broker `ArtifactStore` inside `openwebui_data`.
STT named-volume/factory patterns are reused; STT schema/volume is not the
canonical store. No second engine, global read, product cutover or Gate 3 was
created.

Required next action is host-console recovery: stop only
`broker-reports-doc29-prepare` if running, verify OpenWebUI and both SQLite
stores, then collect read-only pointer/component/capacity accounting. Do not
retry the workload or start Wave 2 cutover.
