# Broker Reports Gate 1 Normalization Synthetic Fixtures

This folder contains synthetic-only fixtures for backend contract tests.

The committed files cover TXT, CSV, duplicate CSV, HTML text, and unsupported
binary-like input. ZIP, XLSX, PDF, DOCX, and image fixtures are generated inside
tests with stdlib helpers to avoid storing binary archives/documents in the
repository.

No customer documents are stored here.
