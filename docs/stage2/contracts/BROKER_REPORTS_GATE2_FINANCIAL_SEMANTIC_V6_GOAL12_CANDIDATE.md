# Broker Reports Gate 2 Financial Semantic V6 Goal 12 Candidate

Status: historical qualification-only contract for prior V6-program Goal 12.
Its GOAL labels do not select a candidate or govern the current Context V2.1
Qualification And Admission program.

## Prerequisite

The terminal Nano V6 receipt is
`MODEL_NOT_SAFE_FOR_SHADOW`, has exactly one committed qualification attempt,
zero hidden retries, zero production admissions, and verified integrity. Nano
is not rerun.

## One exact candidate

The only Goal 12 candidate is
`claude-haiku-4-5-20251001` through provider profile
`anthropic_claude`.

The candidate is published in the live model inventory and is the strongest
candidate already admitted by the repository economy qualification policy.
`gpt-5.6-luna` is not selected because it is outside that policy.

The previous Haiku V5 result remains immutable. Goal 12 uses the new V6
candidate-choice workload, minimal schema, Prompt identity, packet, validator,
materializer, and evidence contract, so it does not repeat the V5 execution
identity.

## Frozen architecture

The single experimental variable is the exact model/provider candidate.
The following remain byte- or hash-identical to the Nano V6 workload:

- Evidence Bundle and candidate compilation contracts;
- Semantic Packet and compact Pack projection;
- Prompt content, version, and hash;
- ambiguity rule;
- choice schema;
- frozen 12-case benchmark and ten semantic call slots;
- deterministic expansion, validation, and total materialization;
- private/safe evidence contracts;
- zero fallback, repair, hidden retry, and production admission.

The terminal runner passes the existing canonical Prompt object to the request
builder. This corrects the historical Goal 11B object seam without changing Prompt
content or any semantic authority.

## Execution boundary

Goal 12 permits one full-scope attempt. Ten semantic cases may invoke the
candidate; two technical-preclose cases make zero provider calls. Exact
private evidence remains outside Git and is linked to a repository-safe
receipt.

The terminal product gate is either `MODEL_SAFE_FOR_SHADOW` or
`MODEL_NOT_SAFE_FOR_SHADOW`. Evidence is mergeable independently of product
status. A failed candidate is not retried under the same identity.

## Later bounded smoke authorization

The terminal Goal 12 full-scope attempt remains immutable and is not rerun.
A later user authorization may create a new repository-revision identity for
exactly the two frozen smoke cases using the same fixed model/provider pair.
That bounded smoke changes no Prompt, Pack, Packet, compiler, Typed Option,
Choice, expected answer, validator, materializer, or case fixture. It is not
a second Goal 12 qualification attempt, publishes no model-safety verdict,
and cannot start the full benchmark.

The new authorization is consumed by exactly two submissions with zero
fallback, repair, or hidden retry. A transparent full-context repository
report is allowed only because both cases are synthetic; exact
actual-corpus context remains private and hash-linked.

The bounded 2026-07-27 smoke passed both frozen cases with exact expected
Choices and a passed technical/replay path. This does not replace or revise
the historical terminal Goal 12 qualification result. It only makes the exact
candidate eligible for a new, separately authorized full V6 qualification
benchmark under the current frozen architecture.
