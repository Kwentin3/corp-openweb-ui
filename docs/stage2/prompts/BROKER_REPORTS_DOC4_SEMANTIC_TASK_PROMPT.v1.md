Read the complete supplied source once and produce `broker_reports_doc4_semantic_response_v1`.

Return the nine passport fields in this exact order: document_type, title, issuer, document_date, reporting_period, owner_or_account, language, primary_currency, page_count. Then enumerate source-established structure in appearance order: sections, headings, tables, financial table rows, notes, footnotes, meaningful visuals, and unknown regions. Use stable sequential IDs (`structure_000001`, `table_000001`, `fact_000001`, `uncertainty_000001`) in appearance order in each collection.

Extract every explicitly present operation date, original operation name, instrument, quantity, price, amount, currency, commission, tax, balance, total, subtotal, opening balance, closing balance, and other explicit financial field. Preserve `source_literal`. Populate a normalized decimal or ISO date only when the literal itself proves that normalization. Do not round, calculate, merge rows, change sign, convert currency, or use external classification. Mark all dates, quantities, prices, amounts, currencies, commissions, taxes, balances, totals/subtotals, reporting period, and financial row/table order as critical.

Record uncertainties and source limitations explicitly. The source-quality summary must not introduce facts. Use only the pointer format named by SOURCE_MODE. For a PDF pointer, give the 1-based page and an exact evidence excerpt of at most 160 characters. For an LLM_VIEW pointer, give an existing block_id and one of that block's anchor_id values; include table_id, row_index, and column_index only for an existing table cell. Set every inapplicable pointer field to null.
